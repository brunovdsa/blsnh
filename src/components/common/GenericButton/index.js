import React, { Component } from 'react';

import './styles.css'

class GenericButton extends Component {
  render() {
    return (
        <div>
            <button>Add</button>
        </div>
    );
  }
}

export default GenericButton;